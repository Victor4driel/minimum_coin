import styled from "styled-components"

export const GroupedCoins = styled.div`
    display: flex;
    flex-direction: row;
    width: ${prop => prop.width};
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
`